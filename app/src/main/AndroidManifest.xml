<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cos30017.a04p.imagemetadata18sp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/activity_metadata_edit_form"> <!--android:theme="@style/AppTheme"-->
        <activity android:name="cos30017.a04p.imagemetadata18sp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="cos30017.a04p.imagemetadata18sp.MetadataEditActivity">

        </activity>
    </application>

</manifest>